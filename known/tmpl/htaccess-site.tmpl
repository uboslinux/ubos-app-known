<IfModule php7_module>
    <Directory "${appconfig.apache2.dir}">
        php_admin_value open_basedir ${appconfig.apache2.dir}:/tmp/:/usr/share/:${appconfig.datadir}/uploads
    </Directory>
</IfModule>
